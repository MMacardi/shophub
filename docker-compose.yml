services:
  backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - db_data:/app/instance
    env_file:
      - ./backend/.env

  frontend:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped
    depends_on:
      - backend

volumes:
  db_data:
